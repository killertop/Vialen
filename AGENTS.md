# Vialen 子代理默认设置

## 公开内容与发布隐私（必须遵循）

- 提交和发布前执行 `python3 scripts/check-public-content.py`；签名后再执行 `python3 scripts/check-public-content.py --apk <正式APK路径>`，两次均通过才可推送或发布。
- 禁止提交签名私钥、密钥库、密码文件、账号凭证、真实订阅地址、个人节点配置、设备标识、个人主目录路径、原始日志、真机截图和数据库。APK 的公开签名证书属于正常验证信息，不是私钥。
- `qa/` 仅保留合成测试夹具和脱敏说明。原始验收证据保存在仓库外；清理时移入系统垃圾篓，不永久删除。
- 正式原生库使用 `bash scripts/build-private-android.sh` 构建，避免 Go 模块元数据嵌入个人工作区路径；仅加 `-trimpath` 不足以保证安全。
- 自动扫描不代替人工审查。公开前检查完整 staged diff、Release 文案和附件清单；含截图时逐张核查。真实令牌、订阅和设备资料不得以测试证据为由公开。
- 已泄露的密钥不得仅靠删文件处理：先确认密钥身份和影响，再处理轮换与历史。重写远端历史或强制更新标签前取得明确授权；普通删除提交不会清除旧历史。

- 后续新启动的子代理默认使用 `model="gpt-6-astra"`、`reasoning_effort="low"`（低）。
- 调用子代理工具时显式填写上述参数；需要覆盖继承设置时使用有限历史或无历史，并补齐必要上下文。
- 用户在当前任务明确指定其他档位时，以用户当前要求为准。
- 本规则取代此前对子代理默认 `medium` 及“不使用低推理档位”的要求，不自动改变已启动子代理的实际设置。

## Android 测试设备

- 日常安装、界面与连接验收优先使用原签名的正式 `com.vialen.app` Release 包，保留用户数据；不能为了测试卸载正式包、换签名或清数据。
- 仅当合成夹具、instrumentation 或破坏性场景无法安全运行于正式包时使用隔离测试包；测试结束后清理本次隔离包，避免长期残留。不得将隔离包结果冒充正式包验收。

- 不主动关闭手机 Wi-Fi，不执行会关闭 Wi-Fi 的网络切换测试；此类历史验收未完成项保留为未验证，不自动补跑。
- Vialen 的 Android 安装、运行、界面、VPN 与验收测试只使用已连接的真机。
- 不创建、安装、启动或依赖 Android Emulator、AVD 或 system image；真机不可用时明确标记为未验证，不能用模拟器结果替代。
- 以用户手机当前的分辨率、显示密度和字号为验收基准；不得为测试修改这些设置，也不得切换显示缩放模式。不同分辨率和大字测试已按用户要求取消，历史结果仅作记录。
- 用户报告显示异常时，先暂停真机验收并处理恢复；仅参数回读一致不能认定显示已完全恢复，还需核对实际异常页面。

## Git 工作树

- 开始和结束任务时都检查 Git 状态；任务完成时不得遗留已修改、已暂存或未跟踪文件。
- 获批改动在验证后做本地提交；无法确认归属的既有内容先保全到 Git stash 或仓库外归档，不直接删除或夹带提交。
- 除非用户明确要求，不向远端 push。

## 分支整洁

- 默认只长期保留 `main` 主分支；普通串行修改直接在 `main` 上完成，不无故创建额外分支。
- 仅在确需并行隔离、独立实验或用户明确要求时创建临时分支；默认使用 `codex/` 前缀。
- 临时分支的获批改动验证完成后，及时合入 `main`，确认没有未合并提交、未提交文件或正在使用的任务后，清理该临时分支及不再使用的关联工作树。
- 开始和结束任务时同时检查分支列表及工作树占用，尽量不遗留 `main` 之外的分支；必须保留时说明原因及剩余事项。
- 历史遗留或归属不明的分支先核对内容与使用状态；未合并成果先保全，不强删分支、不丢弃工作树数据，也不将此规则视为远端删除或 push 授权。
