# 1.7.7 珍珠白主界面视觉对照

final result: passed

通过范围：正式 1.7.7 / 325 在用户确认的全新客户端默认状态下，五个主页面的视觉与导航核验。不是已填充节点、真实订阅更新或 VPN 功能验收，也不替代后续 1.7.8/1.7.9 改动的验收。

## 比较目标与证据

原生 Android 实现，不使用网页预览、模拟器或静态截图冒充实际 UI。视觉稿落地与 design-qa 流程用于逐页实际截图对照；现有原生图标、logo 和字体角色是交互不变约束下的资产依据。

源目录：`/redacted/.codex/generated_images/01a090e9-88dd-7621-b986-8421fc007d0f/`。
最终实现/对照目录：`/redacted/Downloads/Vialen-pearl-1.7.6-20260912-UfuihA/resume-QFm8qe/`。父目录保留首次对照的历史证据，不覆盖旧图。

| 页面 | 源文件 | 实现截图 | 同输入并排证据 |
| --- | --- | --- | --- |
| 首页 | exec-bbde91cb-9828-432f-90ea-16ad764b6a01.png | release-main.png | compare-main.png |
| 分组 | exec-1ba07c33-2877-44a6-9715-053e7f69fa83.png | release-groups.png | compare-groups.png |
| 路由 | exec-fd64b1e9-ae34-4205-ad30-3aa99fe75d75.png | release-routes.png | compare-routes.png |
| 设置 | exec-a8e2c622-8857-45ba-8527-d334c37a8ac2.png | release-settings.png | compare-settings.png |
| 关于 | exec-b782f7cf-89b7-4ad6-9c84-3b5fc35a1cac.png | release-about.png | compare-about.png |

源图 850×1850，手机 1220×2656；实际物理密度 520 dpi、字号 1.0，逻辑视口约 375.38×817.23 dp。CSS viewport / deviceScaleFactor 不适用于原生 Android。比较副本统一至 850×1850 并去掉顶部 110px、底部 50px 的系统栏，单列 850×1690，左右组合 1700×1690；未改变手机显示参数。原始截图以设备分辨率保留。画面和字号仅作等密度视觉对照，不把 AI 图片的细微几何误差视为新的布局规范。

状态差异先行：用户取消顶部文案，且确认以全新客户端空白数据继续。首页源图为有节点/选中状态，实际为空态；分组源图为真实订阅，实际为默认未分组；路由源图有一条启用，实际默认均停用；关于版本 1.7.5 更新为 1.7.7。这些属于用户决定或动态内容差异，不能进行像素级状态一致性宣称。已填充节点的本轮正式版视觉证据仍缺失，契约单元测试不替代该证据。

## Findings

- [已关闭] 首次分组/路由/关于全图对照中的导航图标存疑。2026-09-12 21:34–21:36 在同一正式包等待页面稳定后重新抓取五页，并补充 `compare-navigation.png` 局部并排对照，五页三横线图标均清晰存在，点击可以打开侧栏。未复现持续缺失，无需修改源码；此前证据不足不应定性成已确认的应用缺陷。
- [P3] 路由、分组和关于的 Android 原生 1dp elevation 下沿比生成稿的漫反射阴影更硬；不影响层级、可读性或触控，后续若要进一步逼近可单独收紧原生阴影/描边。当前保持轻量原生实现，没有增加模糊渲染或位图。
- [已解除] 用户回复“继续”后恢复本轮独占真机操作，完成补拍。脚本在每次点击及截图前检查 Vialen 前台，逐页回读标题和导航目标；不使用上轮截错页面作为证据。

## 五项必查表面

- **字体与字号**：沿用 sans-serif / sans-serif-medium 和现有中文系统回退，正文/列表标题 16sp、辅助与分组 14sp；不新增字库，不模拟生成图文字扭曲。路由和设置文字权重、主辅层级、换行正常；关于品牌和六个入口无截断。正文在全图可读，导航图标另有局部比较，避免仅凭缩略全图误判。
- **布局与节奏**：原有页面内边距、卡片 4dp 边距、行高和操作区域保留；设置表面在 RecyclerView 底层绘制，不重新包裹条目或增加偏移。统一 16dp 卡片圆角，密集节点卡 12dp。首页顶部上移是移除文案的明确要求，不是意外布局回归。设置生成稿的行距和导航原点略有近似，不覆盖原生几何约束。
- **色彩与 tokens**：品牌蓝仍为 `#3564E8`，背景 `#F7F8FA`，边缘 `#F0F4FC`，描边 `#E9EDF3`，选中底色 `#F1F5FF`。各主界面透出同一静态底色；白卡片和蓝操作色统一，错误/禁用语义色不改。自动测试检查主/辅助/错误文字对比度不低于 4.5:1。
- **图片与图标**：复用原有 launcher logo、原生 vector 图标和开关；没有用 emoji、CSS/自绘替身或新增生成图替代品牌资产。实际 logo 比生成稿更平整清晰，是保留原品牌资产的有意差异；五页导航图标已通过稳定态截图及局部比较复核。
- **文案与内容**：删除重复顶部导入说明；空态和添加菜单仍说明机场订阅及单节点链接。所有原有路由说明、设置摘要、关于入口保留，未杜撰功能或示例节点。

## 比较历史

1. 应用原生表面并运行自动化；初始编译修正和颜色对比度校正发生在最终截图前，不计为视觉 QA 重测。
2. 用户追加删除顶部文案后，重新构建、签名、安装正式 1.7.7；最终 230 测试和 Release/lint 全过。
3. 打开五组左右并排比较。记录空白客户端/版本/用户删文案差异与 P3 阴影差异；导航图标需稳定态重拍。手机切到其他应用后暂停，不以截错页面补齐验收。
4. 用户继续后，不重新安装或改源码，在仍运行 1.7.7 / 325 的真机重新抓取五页。`resume-QFm8qe/compare-*.png` 为最终五组全图对照，`compare-navigation.png` 为五组局部对照（从上到下：首页、分组、路由、设置、关于；左源图、右真机）。局部每列为统一尺寸图像的 `(0,110,160,130)` 区域，拼图 320×650。已同时打开两类对照检查，不以 UI 树代替图标的可见性证据。
5. 五页“打开导航菜单”均 clickable，原始点击区域 `[0,150][182,332]`，相当于 56×56dp；回到首页收尾。设备仍为 1220×2656、520dpi、字号 1.0，本轮时段未见 Vialen crash 记录。仅补文档，不触发再次升版；侧栏和设置任务的新版本需各自验收。

## 实现与交付检查表

- [x] 五页共享珍珠白视觉，无新增功能/交互重排。
- [x] 顶部冗余文案及中英文闲置资源清除，保护空态和导入菜单说明。
- [x] 正式签名/实际安装包哈希/版本核对；230 单测、Release 构建、lint、16KB 对齐通过。
- [x] 本轮仅使用真机，不改显示参数，不启动 VPN，不导入节点，不操作旧数据库。
- [x] 补拍稳定导航图标与关于页，复查同输入比较后给出最终结论。
- [x] 将已实现并通过构建的代码本地提交，与后续任务明确交接尚未完成的视觉核验；不 push、不发布 GitHub Release。

构建、签名与数据边界详见 `qa/pearl-ui-1.7.7-20260912.md`。


## 1.7.9 侧栏增量验收

final result: passed

本段补充用户选择的第三张珍珠白侧栏；不覆盖上方1.7.7五页验收范围。已在原签名1.7.9/335上完成8项真实导航检查，修复首轮间距偏差并重新截图对照；最终无P0/P1/P2问题。源图、最终截图、五项视觉表面、比较历史和设备/包回读见 [侧栏完整QA](qa/navigation-pearl/README.md)。

## 1.8 轻量白底侧栏（当前选择）

final result: passed

用户不接受上一版分组卡片的真机观感，改选最新第一张“轻量白底”。1.8/340现已覆盖安装：白色平面、260dp抽屉、16sp菜单、选中蓝条、单条分隔线。源图与真机全图、品牌及菜单局部均已同输入比较；8项正式导航和五种可见蓝条状态通过。完整五项视觉检查、P3差异与包/设备回执见[当前侧栏QA](qa/navigation-white/README.md)。本段取代上一版侧栏的当前视觉选择，历史验收记录保留。
